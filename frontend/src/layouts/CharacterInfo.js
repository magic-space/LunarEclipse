import '../assets/App.css';
import { Box, Card, Typography } from "@mui/material";

const CharacterInfo = () => {
    return (
        <Box className='App-CharInfo'>
            <Card>
                <Typography>Character</Typography>
            </Card>
        </Box>
    );
};

export default CharacterInfo;